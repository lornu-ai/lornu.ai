apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: azure-kv-store
  namespace: forgejo
spec:
  provider:
    azurekv:
      authType: WorkloadIdentity
      vaultUrl: "https://dockerworker-kv.vault.azure.net"
      serviceAccountRef:
        name: forgejo-eso-sa
