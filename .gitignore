# === macOS Specific ===
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# === Rust (services/engine) ===
/target/
**/*.rs.bk
Cargo.lock
# Keep Cargo.lock at the root if it's a workspace,
# but ignore local cargo credentials
.cargo/config.toml
.cargo/credentials.toml

# === Bun & TypeScript (apps/web, ci, infra) ===
node_modules/
bun.lockb
# While bun.lockb is often committed, in a "Pure logic"
# monorepo, some prefer letting the CI generate it to ensure parity.
# If you want 100% deterministic builds, comment the line above.

# === Next.js ===
/apps/web/.next/
/apps/web/out/
/apps/web/build/

# === CDK8s & Infrastructure ===
# We synthesize on the fly; never commit the generated YAML
/infra/dist/
/infra/imports/
# Imports are regenerated via 'bun run import:crossplane'

# === Dagger ===
.dagger/
bin/dagger
# If you download the dagger binary locally via script

# === Environment & Secrets (CRITICAL - Zero-Trust Policy) ===
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# GCP Service Account Keys - NEVER commit these!
# Use ADC (gcloud auth application-default login) instead
*-credentials.json
*-service-account.json
**/credentials/*.json
gcloud-*.json

# AWS credentials
.aws/
credentials.csv

# Azure credentials
.azure/
azure*.json

# Terraform state (may contain secrets)
*.tfstate
*.tfstate.*
.terraform/

# === IDEs ===
.vscode/
.idea/
*.swp
*.swo

# === Logs ===
*.log
npm-debug.log*
yarn-debug.log*
bun-debug.log*
