apiVersion: v1
kind: Secret
metadata:
  name: github-webhook-token
  namespace: flux-system
  labels:
    lornu.ai/component: flux
    lornu.ai/managed-by: flux
type: Opaque
stringData:
  token: ${LORNU_FLUX_GITHUB_WEBHOOK_TOKEN}
